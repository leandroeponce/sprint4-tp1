<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agregar Superhéroe</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <% layout('layout') -%>
    <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
        <h2 class="text-2xl font-bold text-center mb-4">Agregar Superhéroe</h2>

        <% if (errores.length > 0) { %>
            <div class="bg-red-100 text-red-700 p-4 rounded mb-4">
                <ul class="list-disc list-inside">
                    <% errores.forEach(error => { %>
                        <li><%= error.msg %></li>
                    <% }) %>
                </ul>
            </div>
        <% } %>

        <form action="/api/heroes/crear" method="POST" class="space-y-4">
            <div>
                <label class="block text-sm font-medium">Nombre del Superhéroe</label>
                <input type="text" name="nombreSuperHeroe" value="<%= datos.nombreSuperHeroe || '' %>" class="w-full border p-2 rounded">
            </div>
            <div>
                <label class="block text-sm font-medium">Nombre Real</label>
                <input type="text" name="nombreReal" value="<%= datos.nombreReal || '' %>" class="w-full border p-2 rounded">
            </div>
            <div>
                <label class="block text-sm font-medium">Edad</label>
                <input type="number" name="edad" value="<%= datos.edad || '' %>" class="w-full border p-2 rounded">
            </div>
            <div>
                <label class="block text-sm font-medium">Planeta de Origen</label>
                <input type="text" name="planetaOrigen" value="<%= datos.planetaOrigen || '' %>" class="w-full border p-2 rounded">
            </div>
            <div>
                <label class="block text-sm font-medium">Debilidad</label>
                <input type="text" name="debilidad" value="<%= datos.debilidad || '' %>" class="w-full border p-2 rounded">
            </div>
            <div>
                <label class="block text-sm font-medium">Poderes (separados por coma)</label>
                <input type="text" name="poderes" value="<%= datos.poderes || '' %>" class="w-full border p-2 rounded">
            </div>
            <div>
                <label class="block text-sm font-medium">Aliados (separados por coma)</label>
                <input type="text" name="aliados" value="<%= datos.aliados || '' %>" class="w-full border p-2 rounded">
            </div>
            <div>
                <label class="block text-sm font-medium">Enemigos (separados por coma)</label>
                <input type="text" name="enemigos" value="<%= datos.enemigos || '' %>" class="w-full border p-2 rounded">
                <button type="submit" class="w-full bg-blue-600 text-white p-2 rounded hover:bg-blue-700 mt-4">Agregar</button>
            </div>
        </form>
    </div>
</body>
</html>

